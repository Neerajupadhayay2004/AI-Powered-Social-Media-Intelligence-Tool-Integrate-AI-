import { t as Route } from "./history._id-Cu7cON9e.js";
import { o as AppShell, r as getById } from "./history-store-CpLXu2WE.js";
import { t as AnalysisResultView } from "./analysis-result-view-B3vNBBgN.js";
import { t as Button } from "./button-CsoKkFFg.js";
import { useEffect, useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { ArrowLeft, Download, FileJson, FileText } from "lucide-react";
//#region src/routes/history.$id.tsx?tsr-split=component
function download(name, data, mime) {
	const blob = new Blob([data], { type: mime });
	const url = URL.createObjectURL(blob);
	const a = document.createElement("a");
	a.href = url;
	a.download = name;
	a.click();
	URL.revokeObjectURL(url);
}
function toCSV(entry) {
	const r = entry.result;
	return [
		["field", "value"],
		["id", entry.id],
		["created_at", new Date(entry.createdAt).toISOString()],
		["source", entry.source],
		["classification", r.classification],
		["classification_confidence", String(r.classification_confidence)],
		["sentiment", r.sentiment],
		["emotion", r.emotion],
		["scam_probability", String(r.scam_probability)],
		["threat_category", r.threat_category],
		["risk_score", String(r.risk_score)],
		["risk_level", r.risk_level],
		["kill_chain_phase", r.kill_chain_phase],
		["keywords", r.keywords?.join("|") ?? ""],
		["summary", r.summary]
	].map((r) => r.map((v) => `"${String(v).replace(/"/g, "\"\"")}"`).join(",")).join("\n");
}
function HistoryDetail() {
	const { id } = Route.useParams();
	const [entry, setEntry] = useState(void 0);
	useEffect(() => {
		setEntry(getById(id));
	}, [id]);
	if (!entry) return /* @__PURE__ */ jsx(AppShell, { children: /* @__PURE__ */ jsxs("div", {
		className: "grid-bg border border-foreground/20 p-16 text-center",
		children: [/* @__PURE__ */ jsx("p", {
			className: "font-mono text-xs uppercase tracking-widest text-muted-foreground",
			children: "Record not found."
		}), /* @__PURE__ */ jsxs(Link, {
			to: "/history",
			className: "mt-4 inline-flex items-center gap-2 border border-foreground bg-foreground px-4 py-2 font-mono text-xs font-bold uppercase tracking-widest text-background",
			children: [/* @__PURE__ */ jsx(ArrowLeft, { className: "h-4 w-4" }), " Back to history"]
		})]
	}) });
	const base = `sentinel-${entry.id.slice(0, 8)}`;
	return /* @__PURE__ */ jsxs(AppShell, { children: [/* @__PURE__ */ jsxs("div", {
		className: "mb-6 flex flex-wrap items-center gap-2",
		children: [
			/* @__PURE__ */ jsxs(Link, {
				to: "/history",
				className: "inline-flex items-center gap-1 font-mono text-[10px] uppercase tracking-widest text-muted-foreground hover:text-foreground",
				children: [/* @__PURE__ */ jsx(ArrowLeft, { className: "h-3 w-3" }), " History"]
			}),
			/* @__PURE__ */ jsxs("span", {
				className: "font-mono text-[10px] uppercase tracking-widest text-muted-foreground",
				children: [
					"/ #",
					entry.id.slice(0, 8),
					" · ",
					new Date(entry.createdAt).toLocaleString(),
					" · ",
					entry.source
				]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "ml-auto flex gap-2",
				children: [
					/* @__PURE__ */ jsxs(Button, {
						variant: "outline",
						size: "sm",
						className: "rounded-none border-foreground/30 font-mono text-[10px] uppercase tracking-widest",
						onClick: () => download(`${base}.json`, JSON.stringify(entry, null, 2), "application/json"),
						children: [/* @__PURE__ */ jsx(FileJson, { className: "mr-1 h-3 w-3" }), " JSON"]
					}),
					/* @__PURE__ */ jsxs(Button, {
						variant: "outline",
						size: "sm",
						className: "rounded-none border-foreground/30 font-mono text-[10px] uppercase tracking-widest",
						onClick: () => download(`${base}.csv`, toCSV(entry), "text/csv"),
						children: [/* @__PURE__ */ jsx(FileText, { className: "mr-1 h-3 w-3" }), " CSV"]
					}),
					/* @__PURE__ */ jsxs(Button, {
						size: "sm",
						className: "rounded-none border border-foreground bg-foreground font-mono text-[10px] uppercase tracking-widest text-background",
						onClick: () => window.print(),
						children: [/* @__PURE__ */ jsx(Download, { className: "mr-1 h-3 w-3" }), " Print / PDF"]
					})
				]
			})
		]
	}), /* @__PURE__ */ jsx(AnalysisResultView, {
		result: entry.result,
		content: entry.content
	})] });
}
//#endregion
export { HistoryDetail as component };
